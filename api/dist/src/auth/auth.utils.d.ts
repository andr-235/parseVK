import type { ExecutionContext } from '@nestjs/common';
import type { Reflector } from '@nestjs/core';
export declare function getBoolMetadata(reflector: Reflector, key: string, context: ExecutionContext): boolean;
