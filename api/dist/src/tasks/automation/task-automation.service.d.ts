import { OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { SchedulerRegistry } from '@nestjs/schedule';
import { CommandBus } from '@nestjs/cqrs';
import type { TaskAutomationRunResponse, TaskAutomationSettingsResponse } from './task-automation.interface.js';
import { UpdateTaskAutomationSettingsDto } from './dto/update-task-automation-settings.dto.js';
import type { ITaskAutomationRepository } from '../interfaces/task-automation-repository.interface.js';
export declare class TaskAutomationService implements OnModuleInit, OnModuleDestroy {
    private readonly repository;
    private readonly commandBus;
    private readonly schedulerRegistry;
    private readonly logger;
    private readonly nextRunJobName;
    private readonly retryTimeoutName;
    private nextRunAt;
    private isExecuting;
    constructor(repository: ITaskAutomationRepository, commandBus: CommandBus, schedulerRegistry: SchedulerRegistry);
    onModuleInit(): Promise<void>;
    onModuleDestroy(): void;
    getSettings(): Promise<TaskAutomationSettingsResponse>;
    updateSettings(dto: UpdateTaskAutomationSettingsDto): Promise<TaskAutomationSettingsResponse>;
    triggerManualRun(): Promise<TaskAutomationRunResponse>;
    private executeAutomation;
    private hasActiveTasks;
    private ensureSettingsExists;
    private getOrCreateSettings;
    private mapToResponse;
    private calculateNextRunDate;
    private scheduleNextRun;
    private scheduleRetry;
    private clearScheduledTimeout;
    private clearRetryTimeout;
    private handleSchedulerNotFound;
    private findLastCompletedTask;
    private extractTaskConfig;
    private formatWithOffset;
    private pad;
    private normalizeMinutes;
}
