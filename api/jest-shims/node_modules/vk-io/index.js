class APIError extends Error {
  constructor({ message = 'VK API error', code = 0 } = {}) {
    super(message);
    this.errorMessage = message;
    this.code = code;
  }
}

class VK {
  constructor() {
    this.api = {
      database: {
        getRegions: async () => ({ items: [], count: 0 }),
        getCities: async () => ({ items: [], count: 0 }),
      },
      groups: {
        search: async () => ({ items: [], count: 0 }),
      },
      wall: {
        get: async () => ({}),
        getById: async () => ({}),
        getComments: async () => ({ items: [], profiles: [], groups: [], count: 0 }),
      },
      users: {
        get: async () => [],
      },
    };
  }
}

module.exports = {
  APIError,
  VK,
};
