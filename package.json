{
  "name": "parsevk",
  "private": true,
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "shadcn": "^3.4.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "api/**/*.ts": [
      "cd api && npm run format:check",
      "cd api && npm run lint"
    ],
    "front/**/*.{ts,tsx,css}": [
      "cd front && npm run format:check",
      "cd front && npm run lint"
    ]
  }
}
