name: Production Integrity

on:
  pull_request:
    paths:
      - ".github/scripts/health-check.sh"
      - ".github/scripts/production/**"
      - ".github/scripts/test_deployment_evidence.py"
      - ".github/scripts/test_post_deploy_smoke.py"
      - ".github/workflows/production-integrity.yml"
      - ".github/workflows/production-verification.yml"
      - ".github/workflows/reusable-record-production-deploy.yml"
      - ".github/scripts/test_storage_guard.py"
      - ".github/scripts/test_production_integrity_contract.py"
      - "scripts/test_production_deploy_observability.py"
      - "docs/deploy-runbook.md"
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-integrity
  cancel-in-progress: false

jobs:
  contract:
    name: Validate Production Integrity Guard
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout proposed guard
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Validate shell and workflow syntax
        run: |
          bash -n .github/scripts/health-check.sh
          bash -n .github/scripts/production/storage-integrity.sh
          bash -n .github/scripts/production/storage-guard.sh
          bash -n .github/scripts/production/preflight.sh
          ruby -e 'require "yaml"; YAML.load_file(ARGV.fetch(0))' \
            .github/workflows/production-verification.yml
          ruby -e 'require "yaml"; YAML.load_file(ARGV.fetch(0))' \
            .github/workflows/reusable-record-production-deploy.yml

      - name: Test storage runtime
        run: python3 .github/scripts/test_storage_guard.py -v

      - name: Test production smoke and evidence runtime
        run: |
          python3 .github/scripts/test_post_deploy_smoke.py -v
          python3 .github/scripts/test_deployment_evidence.py -v

      - name: Test workflow contracts
        run: |
          python3 .github/scripts/test_production_integrity_contract.py -v
          python3 scripts/test_production_deploy_observability.py -v

  integrity:
    name: Check Local Production Integrity
    if: github.event_name != 'pull_request'
    runs-on: [self-hosted]
    timeout-minutes: 10
    env:
      PROJECT_ROOT: /opt/parseVK
      DEPLOYMENT_METADATA_FILE: /opt/parseVK/.deployment-metadata.json
    steps:
      - name: Checkout trusted integrity scripts
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: false
          sparse-checkout: |
            .github/scripts/log-helper.sh
            .github/scripts/production
          sparse-checkout-cone-mode: false

      - name: Verify local storage and rollback releases
        env:
          LOCAL_RELEASE_SCRIPT: ${{ github.workspace }}/.github/scripts/production/local-release.sh
        run: |
          set -euo pipefail
          bash "${{ github.workspace }}/.github/scripts/production/storage-guard.sh" check

      - name: Record successful integrity check
        run: |
          {
            echo "### Production integrity"
            echo
            echo "- local build storage: healthy"
            echo "- current/previous rollback images: verified"
            echo "- registry pulls: not used"
          } >> "$GITHUB_STEP_SUMMARY"
