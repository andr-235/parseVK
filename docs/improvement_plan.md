# 📋 План улучшений: руководство по внедрению

## 🔥 СЕЙЧАС (Критично)

### Убрать утечку памяти в Redis — добавить TTL ко всем кэшам (Выполнено)

Приоритет: Высокий (потенциальная деградация памяти и стабильности, быстрый бизнес-эффект)

Технические шаги:

- Провести инвентаризацию всех кэш-ключей и мест записи в кэш (backend и фоновые задачи).
- Для каждого кэша определить TTL (время жизни ключа) и внедрить его при записи.
- If Redis используется через cache-manager v6+, then применить TTL в миллисекундах при `set`; if используется нативный Redis client, then использовать EX/PX (например, `EX 600`).
- If кэш используется как lock/rate limit, then TTL должен быть коротким (секунды), иначе рискуете «вечными» ключами.
- Добавить метрики количества ключей и среднего TTL для контроля.

Инвентаризация кэш-ключей и TTL:

- `vk:group:{groupId}` — 3600 сек, группы меняются редко.
- `vk:user:batch:{ids}` — 1800 сек, профили обновляются чаще.
- `vk:post:{ownerId}:{offset}:{count}` — 600 сек, посты могут редактироваться.
- `vk:comments:{ownerId}:{postId}:{offset}` — 300 сек, комментарии быстро устаревают.
- `telegram:auth:tx:{transactionId}` — 300 сек, одноразовая транзакция авторизации.
- `rate-limit:{key}` — TTL = окно лимита (мс), по умолчанию 1000 мс.
- `vk-api:metrics` — 3600 сек, агрегированные метрики запросов.
- `circuit-breaker:state:{key}` — resetTimeoutMs + 20% (мс), чтобы не оставлять вечные состояния.
- `circuit-breaker:failures:{key}` — resetTimeoutMs + 20% (мс), чтобы ошибки не копились бесконечно.
- `circuit-breaker:last-failure:{key}` — resetTimeoutMs + 20% (мс), для корректного окна восстановления.
- `circuit-breaker:half-open-calls:{key}` — resetTimeoutMs (мс), короткое окно тестовых вызовов.
- `health:check` — 1 сек, только для проверки доступности Redis.

Метрики Redis:

- `redis_keys_total`
- `redis_avg_ttl_seconds`
- `redis_memory_bytes`
- `redis_keyspace_hit_rate`

Критерии успеха:

- 100% ключей кэша имеют TTL > 0.
- Redis memory usage стабилен (рост < 5%/сутки при стабильной нагрузке).
- Количество ключей не растет бесконтрольно (плато на рабочей нагрузке).

Риски и снижение:

- Риск: Слишком короткий TTL приводит к частым промахам → рост нагрузки.
- Митигация: установить TTL по типам данных и измерять hit rate.
- Риск: Неверные TTL для lock-ключей → повторные задания.
- Митигация: TTL = ожидаемая длительность работы + 20%.

Оценка усилий: 1–2 дня.
Зависимости: нет.

### Исправить таймауты VK API — уменьшить до 30 секунд

Приоритет: Высокий (зависание запросов, блокировка очередей, потери SLA)

Технические шаги:

- Обновить конфиг VK API таймаута до 30 секунд (30000 ms) в едином месте конфигурации.
- If есть разные клиенты (vk-io, fetch, axios), then настроить таймаут на каждом.
- Включить обработку ошибок таймаута с ретраем (ограниченный: 1–2 попытки с backoff).
- Обновить логирование: фиксировать таймауты как отдельную метрику.
- Проверить, что фоновые задания корректно прерываются по таймауту.

Критерии успеха:

- 95% запросов VK API завершаются < 30 сек.
- Таймауты не блокируют очередь задач.
- Ошибки таймаута автоматически ретраятся и не приводят к cascade fail.

Риски и снижение:

- Риск: Сложные запросы VK не успевают → потери данных.
- Митигация: дробить запросы, использовать batching/execute.

Оценка усилий: 0.5–1 день.
Зависимости: нет.

### Добавить пагинацию — для списков комментариев и задач

Приоритет: Высокий (рост данных → невозможность работы интерфейса)

Технические шаги:

- Определить тип пагинации: cursor (рекомендуется для больших списков) или offset.
- If данные часто изменяются, then использовать cursor (устойчивость к вставкам).
- Для комментариев добавить параметры `cursor/limit` или `page/limit` в API.
- Для задач добавить параметры `page/limit` и метаданные (total/pages).
- На фронте подключить lazy loading / pagination UI.

Критерии успеха:

- Время ответа API < 500 ms для первых 50–100 элементов.
- UI не падает при 10k+ элементов.
- Память клиента не растет линейно при скролле.

Риски и снижение:

- Риск: Неверная сортировка ломает cursor.
- Митигация: фиксировать сортировку по (createdAt, id).

Оценка усилий: 2–3 дня.
Зависимости: Таймауты VK API (косвенно), индексы БД (желательно).

## ⚡ НЕДЕЛЯ 1 (Производительность)

### Оптимизировать БД запросы — добавить индексы на часто используемые поля

Приоритет: Средний (ускорение и снижение нагрузки БД, но не критический риск)

Технические шаги:

- Собрать slow queries (логирование > 200 ms).
- Определить поля для индекса: фильтры, сортировки, join.
- Добавить индексы типа B-Tree для стандартных запросов.
- If есть поиск по JSON/array, then использовать GIN/GiST.
- Прогнать explain analyze на ключевых запросах.

Критерии успеха:

- P95 времени БД запросов < 200 ms для основных эндпоинтов.
- CPU/IO БД снижаются при той же нагрузке.

Риски и снижение:

- Риск: Индексы увеличат размер БД.
- Митигация: индексировать только реально используемые поля.

Оценка усилий: 1–2 дня.
Зависимости: логирование запросов.

### Добавить кэш для статистики — кэшировать результаты подсчетов на 10 минут

Приоритет: Средний (снижение нагрузки, улучшение отклика)

Технические шаги:

- Определить критичные статистики (count/sum/aggregate).
- Кэшировать результаты с TTL = 10 минут (600 секунд).
- If Redis версия поддерживает PX, then TTL = 600000 ms; else EX 600.
- Добавить инвалидацию при изменении данных (по ключевым событиям).
- Добавить мониторинг hit/miss.

Критерии успеха:

- Hit rate кэша > 70% для статистики.
- Время ответа статистики < 200 ms.

Риски и снижение:

- Риск: Устаревшие данные.
- Митигация: TTL 10 минут + invalidation по ключевым событиям.

Оценка усилий: 1–2 дня.
Зависимости: TTL для Redis.

### Исправить фронтенд рендеры — оптимизировать тяжелые компоненты

Приоритет: Средний (ускорение UX, но не блокер)

Технические шаги:

- Профилировать React рендеры (React DevTools).
- Добавить memo/useMemo/useCallback на тяжёлые компоненты и вычисления.
- If списки большие, then использовать виртуализацию (например, react-window).
- Разбить компоненты на более мелкие с контролируемыми пропсами.
- Включить lazy loading для тяжелых модулей.

Критерии успеха:

- Время рендера ключевых страниц < 200 ms.
- FPS > 50 при скролле таблиц.

Риски и снижение:

- Риск: Мемоизация усложняет код.
- Митигация: применять только к реально тяжёлым узлам.

Оценка усилий: 2–3 дня.
Зависимости: пагинация (для снижения объема данных).

## 🛡️ НЕДЕЛЯ 2 (Надежность)

### Добавить rate limiting — защитить от перегрузки API

Приоритет: Средний (снижает риски DoS и перегрузки VK API)

Технические шаги:

- Включить rate limit на API уровне (например, 60 req/min для публичных эндпоинтов).
- If несколько инстансов, then использовать Redis-based limiter (глобальный лимит).
- Настроить отдельные лимиты для VK API запросов и пользовательских.
- Добавить fallback: если лимит превышен, вернуть 429 и retry-after.
- Логировать превышения лимита.

Критерии успеха:

- 0 необработанных всплесков нагрузки.
- Четкие ответы 429 при превышении.

Риски и снижение:

- Риск: Ложные блокировки.
- Митигация: исключения для trusted API ключей/админов.

Оценка усилий: 1–2 дня.
Зависимости: Redis TTL и конфигурация.

### Валидировать все входные данные — от VK API и пользователей

Приоритет: Средний (стабильность и безопасность)

Технические шаги:

- Ввести строгие DTO схемы для входных данных.
- If входные данные приходят от VK API, then валидировать форматы и обязательные поля.
- Включить централизованный validation pipe.
- Добавить логирование ошибок валидации.
- Обновить тесты на граничные кейсы.

Критерии успеха:

- 100% запросов проходят валидацию по схеме.
- Ошибки валидируются на уровне API (400).

Риски и снижение:

- Риск: Новые ограничения ломают клиентов.
- Митигация: вводить постепенно, документировать.

Оценка усилий: 2–3 дня.
Зависимости: нет.

### Добавить health checks — для мониторинга состояния сервисов

Приоритет: Средний (важно для стабильности, но не критично прямо сейчас)

Технические шаги:

- Добавить endpoint `/health` и `/ready`.
- Проверять подключение к БД, Redis и VK API.
- If сервисы внешние недоступны, then отдавать degraded статус.
- Интегрировать с мониторингом (Prometheus/Grafana).

Критерии успеха:

- Health endpoint отвечает < 200 ms.
- Monitoring получает корректные статусы.

Риски и снижение:

- Риск: Ложные тревоги от внешних сервисов.
- Митигация: separate checks (liveness vs readiness).

Оценка усилий: 1–2 дня.
Зависимости: нет.

## 📊 НЕДЕЛЯ 3 (Масштабирование)

### Оптимизировать очередь задач — уменьшить нагрузку на Redis

Приоритет: Низкий (масштабирование, не срочно)

Технические шаги:

- Проверить настройки очереди (concurrency, retry, backoff).
- If Redis перегружен, then уменьшить количество активных job’ов.
- Включить batch processing и уменьшить количество мелких job’ов.
- Очистить/архивировать завершенные задачи.

Критерии успеха:

- Redis CPU < 70% при пиковой нагрузке.
- Очередь не накапливает тысячи job’ов.

Риски и снижение:

- Риск: Снижение throughput.
- Митигация: балансировать concurrency и batch size.

Оценка усилий: 2–3 дня.
Зависимости: TTL в Redis.

### Добавить логирование ошибок — для отладки проблем

Приоритет: Низкий (важно, но не критично)

Технические шаги:

- Ввести централизованный error logger.
- Добавить correlation id в запросы.
- If ошибка в фоновой задаче, then логировать контекст (taskId, userId).
- Настроить алерты на critical ошибки.

Критерии успеха:

- Ошибки доступны в логах ≤ 5 секунд после события.
- Есть минимум 1 алерт на critical ошибки.

Риски и снижение:

- Риск: Логи будут переполнены.
- Митигация: sampling и уровни логирования.

Оценка усилий: 1–2 дня.
Зависимости: нет.

### Тестировать с большой нагрузкой — проверить на 1000+ комментариев

Приоритет: Низкий (масштабирование/проверка)

Технические шаги:

- Создать сценарий нагрузки с 1000+ комментариями.
- If нет генератора данных, then использовать тестовые фикстуры.
- Проверить производительность UI и API.
- Замерить latency, память, CPU.

Критерии успеха:

- Время ответа API < 1 сек при 1000+ комментариях.
- UI остается отзывчивым.

Риски и снижение:

- Риск: тесты перегружают прод.
- Митигация: запуск только в staging.

Оценка усилий: 1–2 дня.
Зависимости: пагинация, индексы.

## 🔄 ПОСТОЯННО (Поддержка)

### Мониторить использование Redis — следить за памятью и ключами

Приоритет: Средний (устойчивость инфраструктуры)

Технические шаги:

- Настроить метрики памяти Redis.
- If память > 80%, then алерт.
- Отслеживать количество ключей по префиксам.

Критерии успеха:

- Алерты срабатывают корректно.
- Память не растет без контроля.

Риски и снижение:

- Риск: пропуск деградации.
- Митигация: автоматизированные алерты.

Оценка усилий: 0.5–1 дня.
Зависимости: TTL для Redis.

### Обновлять VK токены — вовремя продлевать доступ

Приоритет: Средний (устойчивость интеграции)

Технические шаги:

- Ввести календарный контроль сроков токенов.
- If токен истекает < 7 дней, then уведомить.
- Проверять валидность токенов при старте.

Критерии успеха:

- 0 отказов VK API из‑за истечения токенов.

Риски и снижение:

- Риск: потеря доступа при забытом обновлении.
- Митигация: автоматические напоминания.

Оценка усилий: 0.5 дня.
Зависимости: нет.

### Делать бэкапы БД — еженедельно сохранять данные

Приоритет: Средний (защита данных)

Технические шаги:

- Настроить регулярные backups (еженедельно).
- If backup не выполнен, then уведомить.
- Проверить восстановление из backup (test restore).

Критерии успеха:

- 100% недельных бэкапов успешны.
- Время восстановления < 1 час.

Риски и снижение:

- Риск: backup поврежден.
- Митигация: периодический restore test.

Оценка усилий: 1 день.
Зависимости: нет.

## 🎯 ПРИОРИТЕТЫ (обоснование)

Высокий: Утечка памяти, таймауты, пагинация
Бизнес-эффект: стабильность и доступность ключевых функций.
Техническая срочность: напрямую влияет на SLA и надежность.

Средний: Оптимизация БД, кэширование, безопасность
Улучшает скорость, снижает нагрузку, но не вызывает падений немедленно.

Низкий: Масштабирование, продвинутый мониторинг
Требуется для роста, но можно выполнять после стабилизации.

## Ожидаемые результаты

- Снижение времени ответа API минимум на 30%.
- Контролируемое использование Redis и БД.
- Улучшение UX за счет стабильной пагинации и оптимизации рендеров.
- Готовность к росту нагрузки без потери стабильности.
